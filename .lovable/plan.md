- Goal\nRe-enable authentication so every page (except login/password reset) and every database/edge-function action requires a logged-in user.\n\n## Current state\n- `AuthProvider`, `useAuth`, `ProtectedRoute`, and a working `/login` page already exist.\n- `App.tsx` currently renders all routes unwrapped, byp